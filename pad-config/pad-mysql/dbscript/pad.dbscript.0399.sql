USE pad;

-- below scripts have already been applied to dev and live 

-- CREATE TABLE `pad`.`account_numbers` (
--   `account_id` INT NOT NULL AUTO_INCREMENT,
--   `number` INT NOT NULL,
--   PRIMARY KEY (`account_id`, `number`),
--   UNIQUE INDEX `account_id_UNIQUE` (`account_id` ASC),
--   UNIQUE INDEX `number_UNIQUE` (`number` ASC))
-- ENGINE = InnoDB
-- AUTO_INCREMENT = 1000
-- DEFAULT CHARACTER SET = utf8
-- COLLATE = utf8_bin;


-- !!!!!@@@@@ RUN account_number.sql SCRIPT @@@@@!!!!! - Already applied to dev and live